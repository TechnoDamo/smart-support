# Smart Support Frontend

Next.js фронтенд для operator workspace (`Inbox`, `Knowledge Base`, `Settings`) с режимами `live` и `mock`.

## Запуск

```bash
npm install
npm run dev
```

API base URL настраивается через `NEXT_PUBLIC_SUPPORT_API_BASE_URL`.
